{
  "name": "dice-game-project",
  "private": true,
  "scripts": {
    "lint": "npm --prefix api run lint && npm --prefix web run lint",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "npm --prefix api test -- --runInBand && npm --prefix web run test",
    "test:e2e": "npm --prefix api run test:e2e -- --runInBand",
    "build": "npm --prefix api run build && npm --prefix web run build",
    "verify": "npm run lint && npm run test && npm run build"
  }
}
